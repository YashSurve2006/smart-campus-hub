/**
 * Placeholder for future on-device / custom inference (e.g. llama.cpp binding).
 * Same contract as openaiChat / ollamaChat.
 */
export async function localChat() {
  throw new Error('Local provider not configured');
}
