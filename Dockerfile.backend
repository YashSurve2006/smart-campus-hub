# Smart Campus Hub — API
FROM node:20-alpine
WORKDIR /app

COPY backend/package.json backend/package-lock.json ./
RUN npm ci --omit=dev

COPY backend/ ./

RUN mkdir -p uploads/notices uploads/events uploads/avatars

ENV NODE_ENV=production
EXPOSE 5000
CMD ["node", "server.js"]
